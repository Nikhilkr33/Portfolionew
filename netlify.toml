[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_ENV = "production"

[build.processing]
  skip_processing = false

[cache]
  # Cache node modules between builds for faster deploys
  paths = ["node_modules/**"]
